<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>CBA Store — boxingcba.com</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:wght@400;700;900&family=Barlow+Condensed:wght@700;900&display=swap" rel="stylesheet">
<style>
:root{--gold:#d4a017;--dark:#06060c;--dark2:#0d0d18;--dark3:#12121f;--dark4:#1a1a2a;--border:rgba(212,160,23,.15);--text:#e8e8f0;--muted:#7a7a9a}
*{margin:0;padding:0;box-sizing:border-box}
body{background:var(--dark);color:var(--text);font-family:'Barlow',sans-serif;min-height:100vh;overflow-x:hidden}
::-webkit-scrollbar{width:4px}::-webkit-scrollbar-track{background:var(--dark)}::-webkit-scrollbar-thumb{background:var(--gold);border-radius:2px}
/* HEADER */
header{position:sticky;top:0;z-index:100;background:rgba(6,6,12,.97);backdrop-filter:blur(20px);border-bottom:1px solid var(--border);padding:0 16px}
.hdr{max-width:1100px;margin:0 auto;display:flex;align-items:center;height:58px;gap:12px}
.logo{display:flex;align-items:center;gap:8px;text-decoration:none;flex-shrink:0}
.logo-icon{width:34px;height:34px;background:linear-gradient(135deg,#1a1000,#2a1800);border:1.5px solid var(--gold);border-radius:8px;display:flex;align-items:center;justify-content:center;font-family:'Bebas Neue';font-size:12px;color:var(--gold);letter-spacing:1px}
.logo-name{font-family:'Bebas Neue';font-size:16px;letter-spacing:2px;color:#fff}
.logo-sub{font-size:8px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px}
.hdr-search{flex:1;position:relative}
.hdr-inp{width:100%;background:rgba(255,255,255,.05);border:1.5px solid rgba(212,160,23,.2);border-radius:10px;padding:9px 44px 9px 14px;color:#fff;font-family:'Barlow';font-size:13px;outline:none;transition:.3s}
.hdr-inp:focus{border-color:var(--gold);background:rgba(212,160,23,.04)}
.hdr-inp::placeholder{color:var(--muted)}
.hdr-btn{position:absolute;right:6px;top:50%;transform:translateY(-50%);background:linear-gradient(135deg,#c49010,#f0c030);border:none;border-radius:7px;width:30px;height:30px;cursor:pointer;font-size:14px}
.lang-wrap{display:flex;gap:3px;flex-shrink:0}
.lng{background:transparent;border:1px solid rgba(255,255,255,.1);color:var(--muted);border-radius:5px;padding:4px 9px;font-size:10px;cursor:pointer;font-family:'Barlow Condensed';letter-spacing:1px;transition:.2s}
.lng.active,.lng:hover{border-color:var(--gold);color:var(--gold)}
/* HERO */
.hero{background:linear-gradient(135deg,#04040a,#0a0800,#04040a);padding:36px 16px;text-align:center;position:relative;overflow:hidden}
.hero::before{content:'';position:absolute;inset:0;background:radial-gradient(ellipse 80% 60% at 50% 50%,rgba(212,160,23,.05),transparent)}
.hero-tag{display:inline-block;background:rgba(212,160,23,.1);border:1px solid rgba(212,160,23,.25);color:var(--gold);font-family:'Barlow Condensed';font-size:10px;letter-spacing:4px;padding:5px 14px;border-radius:20px;margin-bottom:12px}
.hero-title{font-family:'Bebas Neue';font-size:clamp(28px,7vw,52px);letter-spacing:3px;color:#fff;margin-bottom:6px;line-height:1}
.hero-title span{background:linear-gradient(135deg,#d4a017,#f5d060);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-sub{color:var(--muted);font-size:13px;max-width:480px;margin:0 auto 20px;line-height:1.6}
.hero-srch{max-width:540px;margin:0 auto;position:relative}
.hero-inp{width:100%;background:rgba(255,255,255,.05);border:2px solid rgba(212,160,23,.25);border-radius:12px;padding:13px 130px 13px 18px;color:#fff;font-family:'Barlow';font-size:14px;outline:none;transition:.3s}
.hero-inp:focus{border-color:var(--gold);background:rgba(212,160,23,.05)}
.hero-inp::placeholder{color:var(--muted)}
.hero-go{position:absolute;right:6px;top:50%;transform:translateY(-50%);background:linear-gradient(135deg,#c49010,#f0c030);border:none;border-radius:9px;padding:8px 18px;color:#000;font-weight:900;font-family:'Barlow Condensed';font-size:12px;letter-spacing:2px;cursor:pointer;transition:.2s;white-space:nowrap}
.hero-go:hover{transform:translateY(-50%) translateY(-2px);box-shadow:0 6px 20px rgba(212,160,23,.4)}
.hero-hint{font-size:10px;color:var(--muted);margin-top:8px}
/* VISUAL SEARCH */
.vsearch{display:flex;gap:8px;justify-content:center;margin-top:14px;flex-wrap:wrap}
.vs-btn{background:rgba(255,255,255,.05);border:1.5px solid rgba(255,255,255,.12);color:var(--text);border-radius:10px;padding:10px 20px;font-family:'Barlow Condensed';font-size:12px;letter-spacing:2px;cursor:pointer;transition:.25s;display:inline-flex;align-items:center;gap:6px}
.vs-btn:hover{border-color:rgba(212,160,23,.4);color:var(--gold)}
/* SECTION */
.sec{padding:28px 16px}
.sec-inner{max-width:1100px;margin:0 auto}
.sec-hdr{display:flex;align-items:center;gap:10px;margin-bottom:16px}
.sec-ttl{font-family:'Bebas Neue';font-size:18px;letter-spacing:3px;color:#fff}
.sec-line{flex:1;height:1px;background:var(--border)}
.sec-tag{background:rgba(212,160,23,.08);border:1px solid rgba(212,160,23,.18);color:var(--gold);font-size:9px;font-family:'Barlow Condensed';letter-spacing:2px;padding:3px 9px;border-radius:4px}
/* ACTION BUTTONS */
.ab-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(130px,1fr));gap:8px;margin-bottom:24px}
.ab{background:var(--dark3);border:1.5px solid rgba(212,160,23,.15);border-radius:12px;padding:14px 10px;cursor:pointer;text-align:center;transition:.3s;color:var(--text)}
.ab:hover{border-color:rgba(212,160,23,.4);background:rgba(212,160,23,.06);transform:translateY(-3px)}
.ab-icon{font-size:22px;display:block;margin-bottom:6px}
.ab-lbl{font-family:'Bebas Neue';font-size:13px;letter-spacing:2px;color:#fff;display:block}
.ab-sub{font-size:9px;color:var(--muted);display:block;margin-top:2px}
/* CATEGORIES */
.cats{display:flex;flex-wrap:wrap;gap:7px}
.cat{background:var(--dark3);border:1px solid rgba(255,255,255,.06);border-radius:18px;padding:7px 14px;cursor:pointer;transition:.22s;display:flex;align-items:center;gap:5px;color:var(--text);font-size:12px;border:none;font-family:'Barlow'}
.cat:hover{background:rgba(212,160,23,.1);color:var(--gold)}
/* FEAT GRID */
.feat-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:9px}
.feat{background:var(--dark3);border:1px solid rgba(255,255,255,.05);border-radius:11px;padding:14px 10px;text-align:center;cursor:pointer;transition:.3s}
.feat:hover{border-color:rgba(212,160,23,.28);transform:translateY(-3px);box-shadow:0 12px 30px rgba(0,0,0,.4)}
.feat-ic{font-size:26px;margin-bottom:6px}
.feat-nm{font-size:11px;color:var(--text);font-family:'Barlow Condensed';letter-spacing:1px;font-weight:700}
.feat-sb{font-size:9px;color:var(--muted);margin-top:2px}
/* PRODUCT CARD */
.prod-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(155px,1fr));gap:11px}
.pc{background:var(--dark3);border:1px solid rgba(255,255,255,.05);border-radius:11px;overflow:hidden;transition:.3s;cursor:pointer;text-decoration:none;display:block;position:relative}
.pc:hover{border-color:rgba(212,160,23,.25);transform:translateY(-4px);box-shadow:0 16px 40px rgba(0,0,0,.5)}
.pc-badge{background:linear-gradient(135deg,#c49010,#f0c030);color:#000;font-size:9px;font-weight:900;font-family:'Barlow Condensed';letter-spacing:2px;padding:3px 8px}
.pc-body{padding:11px}
.pc-em{font-size:30px;display:block;margin-bottom:5px}
.pc-nm{font-size:11px;font-weight:900;color:#fff;font-family:'Barlow Condensed';letter-spacing:1px;margin-bottom:3px;line-height:1.3}
.pc-ds{font-size:10px;color:var(--muted);line-height:1.5;margin-bottom:7px}
.pc-cta{background:rgba(212,160,23,.12);border:1px solid rgba(212,160,23,.22);color:var(--gold);font-size:10px;font-family:'Barlow Condensed';letter-spacing:1px;font-weight:900;padding:5px 10px;border-radius:5px;display:block;text-align:center}
.pc-share{position:absolute;top:32px;right:7px;background:rgba(37,211,102,.15);border:1px solid rgba(37,211,102,.3);border-radius:5px;padding:3px 7px;font-size:11px;cursor:pointer;color:#25D366;text-decoration:none;display:none;z-index:2}
.pc:hover .pc-share{display:block}
/* POTW */
.potw{background:linear-gradient(135deg,rgba(212,160,23,.09),rgba(212,160,23,.03));border:1.5px solid rgba(212,160,23,.3);border-radius:14px;padding:18px;margin-bottom:20px;position:relative;overflow:hidden}
.potw::after{content:'⭐ PRODUCTO DE LA SEMANA';position:absolute;top:10px;right:12px;font-size:8px;font-family:'Barlow Condensed';letter-spacing:3px;color:var(--gold);font-weight:900}
.potw-nm{font-family:'Bebas Neue';font-size:22px;color:#fff;letter-spacing:2px;margin-bottom:4px}
.potw-ds{font-size:12px;color:#aaa;line-height:1.6;margin-bottom:12px}
/* MODALS */
.overlay{position:fixed;inset:0;background:rgba(4,4,10,.96);backdrop-filter:blur(16px);z-index:300;display:none;align-items:center;justify-content:center;padding:16px}
.overlay.on{display:flex}
.mbox{background:linear-gradient(135deg,#0d0d1a,#14100a);border:1.5px solid rgba(212,160,23,.28);border-radius:18px;padding:22px;width:100%;max-width:440px;max-height:88vh;overflow-y:auto}
.mttl{font-family:'Bebas Neue';font-size:20px;letter-spacing:3px;color:#fff;margin-bottom:3px}
.msub{font-size:11px;color:var(--muted);margin-bottom:18px}
.mcls{float:right;background:rgba(255,255,255,.07);border:none;color:#888;border-radius:7px;padding:5px 11px;cursor:pointer;font-size:13px;margin-top:-3px}
/* QUIZ */
.qstep{display:none}.qstep.on{display:block}
.qprog{display:flex;gap:4px;margin-bottom:14px}
.qdot{flex:1;height:3px;border-radius:2px;background:rgba(255,255,255,.1)}.qdot.done{background:var(--gold)}
.qopts{display:flex;flex-direction:column;gap:7px;margin:10px 0}
.qopt{background:rgba(255,255,255,.04);border:1.5px solid rgba(255,255,255,.08);border-radius:9px;padding:12px 14px;cursor:pointer;font-family:'Barlow';font-size:12px;color:var(--text);transition:.22s;text-align:left}
.qopt:hover,.qopt.sel{background:rgba(212,160,23,.09);border-color:rgba(212,160,23,.4);color:#fff}
/* CALC */
.cinp{width:100%;background:rgba(10,10,20,.8);border:1.5px solid rgba(212,160,23,.2);border-radius:9px;padding:11px 14px;color:#fff;font-family:'Barlow';font-size:16px;text-align:center;outline:none;margin:10px 0}
.cinp:focus{border-color:var(--gold)}
.ctog{display:flex;gap:4px;margin-bottom:10px}
.cunit{flex:1;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08);border-radius:7px;padding:7px;cursor:pointer;font-family:'Barlow Condensed';font-size:11px;letter-spacing:1px;color:var(--muted);text-align:center;transition:.2s}
.cunit.on{background:rgba(212,160,23,.1);border-color:rgba(212,160,23,.3);color:var(--gold)}
.crow{display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px solid rgba(255,255,255,.05)}
.crow:last-child{border:none}
.clbl{font-size:11px;color:var(--muted)}
.cval{font-family:'Bebas Neue';font-size:17px;color:var(--gold);letter-spacing:1px}
/* BUDGET */
.bslider{width:100%;-webkit-appearance:none;height:5px;border-radius:3px;background:rgba(212,160,23,.2);outline:none;margin:14px 0;cursor:pointer}
.bslider::-webkit-slider-thumb{-webkit-appearance:none;width:20px;height:20px;border-radius:50%;background:var(--gold);cursor:pointer;border:2px solid #000;box-shadow:0 0 10px rgba(212,160,23,.5)}
.bnum{font-family:'Bebas Neue';font-size:38px;color:var(--gold);text-align:center;letter-spacing:2px}
.kitem{display:flex;align-items:center;gap:10px;padding:9px;background:rgba(255,255,255,.02);border-radius:7px;margin-bottom:5px;border-left:3px solid transparent;transition:.3s}
.kitem.in{border-left-color:#2ecc71}
.kitem.out{opacity:.3}
.kim{font-size:18px;width:24px;text-align:center}
.kinf{flex:1}
.knm{font-size:11px;font-weight:900;color:#fff;font-family:'Barlow Condensed';letter-spacing:1px}
.kpr{font-size:10px;color:var(--muted)}
/* ADMIN */
.adm-btn{position:fixed;bottom:14px;left:14px;background:rgba(0,0,0,.7);border:1px solid rgba(255,255,255,.04);border-radius:7px;padding:5px 9px;font-size:9px;color:rgba(255,255,255,.12);cursor:pointer;z-index:50;font-family:'Barlow Condensed'}
.adm-btn:hover{color:var(--gold);border-color:rgba(212,160,23,.3)}
/* AI OVERLAY */
.ai-ov{position:fixed;inset:0;background:rgba(4,4,10,.96);backdrop-filter:blur(20px);z-index:400;display:none;flex-direction:column;align-items:center;justify-content:center}
.ai-ov.on{display:flex}
.ai-spin{font-size:44px;animation:spin 1.5s linear infinite;margin-bottom:14px}
@keyframes spin{to{transform:rotate(360deg)}}
.ai-res{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:rgba(8,8,16,.97);backdrop-filter:blur(20px);border:1px solid rgba(212,160,23,.3);border-radius:14px;padding:18px;z-index:410;min-width:280px;max-width:88vw;display:none;animation:fup .4s ease}
@keyframes fup{from{opacity:0;transform:translateX(-50%) translateY(20px)}to{opacity:1;transform:translateX(-50%) translateY(0)}}
/* FOOTER */
footer{background:var(--dark2);border-top:1px solid var(--border);padding:20px 16px;text-align:center}
.flogo{font-family:'Bebas Neue';font-size:20px;letter-spacing:3px;background:linear-gradient(135deg,#d4a017,#f5d060);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:8px}
.flinks{display:flex;justify-content:center;gap:14px;flex-wrap:wrap;margin-bottom:8px}
.flinks a{color:var(--muted);text-decoration:none;font-size:11px;font-family:'Barlow Condensed';letter-spacing:1px;transition:.2s}
.flinks a:hover{color:var(--gold)}
.fcopy{font-size:10px;color:var(--muted)}
@media(max-width:480px){.logo-name{font-size:13px}.lang-wrap{display:none}.feat-grid{grid-template-columns:repeat(3,1fr)}.prod-grid{grid-template-columns:repeat(2,1fr)}}

/* FASHION ADVISOR */
.fopt{background:rgba(255,255,255,.04);border:1.5px solid rgba(255,255,255,.08);border-radius:8px;padding:8px 12px;cursor:pointer;font-family:'Barlow';font-size:12px;color:var(--text);transition:.2s}
.fopt.on,.fopt:hover{background:rgba(212,160,23,.1);border-color:rgba(212,160,23,.4);color:#fff}
.fcol{background:rgba(255,255,255,.04);border:1.5px solid rgba(255,255,255,.07);border-radius:16px;padding:5px 10px;cursor:pointer;font-family:'Barlow';font-size:11px;color:var(--text);transition:.2s}
.fcol.on,.fcol:hover{background:rgba(212,160,23,.1);border-color:rgba(212,160,23,.35);color:var(--gold)}
.fcat{background:var(--dark3);border:1.5px solid rgba(255,255,255,.06);border-radius:11px;padding:14px 12px;cursor:pointer;transition:.3s;text-decoration:none;display:block;margin-bottom:8px}
.fcat:hover{border-color:rgba(212,160,23,.3);transform:translateX(4px)}
.fcat-hdr{display:flex;align-items:center;gap:10px;margin-bottom:4px}
.fcat-icon{font-size:22px}
.fcat-name{font-family:'Bebas Neue';font-size:16px;color:#fff;letter-spacing:1px}
.fcat-detail{font-size:10px;color:var(--muted);font-family:'Barlow';margin-left:32px}
.fcat-cta{float:right;background:linear-gradient(135deg,#f90,#f60);color:#fff;border-radius:5px;padding:4px 8px;font-size:9px;font-weight:900;font-family:'Barlow Condensed';letter-spacing:1px;margin-top:-24px}
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <div class="hdr">
    <a href="#" class="logo">
      <div class="logo-icon">CBA</div>
      <div><div class="logo-name">CBA STORE</div><div class="logo-sub">boxingcba.com</div></div>
    </a>
    <div class="hdr-search">
      <input class="hdr-inp" id="h-inp" placeholder="Busca cualquier producto en Amazon..." onkeydown="if(event.key==='Enter')goSearch(this.value)">
      <button class="hdr-btn" onclick="goSearch(document.getElementById('h-inp').value)">🔍</button>
    </div>
    <div class="lang-wrap">
      <button class="lng active" id="lng-es" onclick="setL('es',this)">ES</button>
      <button class="lng" id="lng-en" onclick="setL('en',this)">EN</button>
    </div>
  </div>
</header>

<!-- HERO -->
<div class="hero">
  <div class="hero-tag" id="htag">🥊 TIENDA OFICIAL — CUBAN BOXING ACADEMY</div>
  <h1 class="hero-title"><span id="ht1">ENCUENTRA</span> <span id="ht2">TODO</span></h1>
  <p class="hero-sub" id="hsub">Busca cualquier producto y cómpralo en Amazon. Boxeo, electrónicos, ropa, hogar y más.</p>
  <div class="hero-srch">
    <input class="hero-inp" id="hero-inp" placeholder="¿Qué estás buscando?" onkeydown="if(event.key==='Enter')goSearch(this.value)">
    <button class="hero-go" id="hgo" onclick="goSearch(document.getElementById('hero-inp').value)">BUSCAR →</button>
  </div>
  <p class="hero-hint" id="hhint">Presiona Enter — te lleva directo a Amazon</p>
  <div class="vsearch">
    <button class="vs-btn" onclick="openCam()">📷 <span class="cam-t">TOMAR FOTO</span></button>
    <button class="vs-btn" onclick="openGal()">🖼️ <span class="gal-t">SUBIR FOTO</span></button>
  </div>
  <input type="file" id="f-cam" accept="image/*" capture="environment" style="display:none" onchange="doVS(this)">
  <input type="file" id="f-gal" accept="image/*" style="display:none" onchange="doVS(this)">
</div>

<!-- AI OVERLAY -->
<div class="ai-ov" id="ai-ov">
  <div class="ai-spin">🤖</div>
  <div style="font-family:'Bebas Neue';font-size:20px;color:var(--gold);letter-spacing:3px" id="ai-st">ANALIZANDO FOTO...</div>
  <div style="font-size:12px;color:var(--muted);margin-top:6px" id="ai-sb">IA identificando el producto</div>
</div>
<div class="ai-res" id="ai-res">
  <div style="font-size:9px;color:var(--gold);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:5px">🤖 IA IDENTIFICÓ:</div>
  <div id="ai-name" style="font-family:'Bebas Neue';font-size:19px;color:#fff;letter-spacing:1px;margin-bottom:12px"></div>
  <div style="display:flex;gap:7px">
    <button onclick="doAISearch()" style="flex:1;background:linear-gradient(135deg,#c49010,#f0c030);color:#000;border:none;border-radius:9px;padding:11px;font-weight:900;font-size:12px;cursor:pointer;font-family:'Barlow Condensed';letter-spacing:2px">BUSCAR EN AMAZON →</button>
    <button onclick="closeAI()" style="background:rgba(255,255,255,.07);color:#888;border:none;border-radius:9px;padding:11px 13px;font-size:14px;cursor:pointer">✕</button>
  </div>
</div>

<!-- MAIN -->
<div class="sec">
<div class="sec-inner">

  <!-- PRODUCT OF THE WEEK -->
  <div id="potw" style="display:none">
    <div class="potw">
      <div style="background:var(--gold);color:#000;font-size:9px;font-weight:900;font-family:'Barlow Condensed';letter-spacing:3px;padding:3px 9px;border-radius:4px;display:inline-block;margin-bottom:10px">⭐ COACH PICK</div>
      <div class="potw-nm" id="pw-nm"></div>
      <div class="potw-ds" id="pw-ds"></div>
      <a id="pw-lk" href="#" target="_blank" style="display:inline-flex;align-items:center;gap:6px;background:linear-gradient(135deg,#f90,#f60);color:#fff;border-radius:9px;padding:10px 18px;font-weight:900;font-size:11px;text-decoration:none;font-family:'Barlow Condensed';letter-spacing:2px">🛒 VER EN AMAZON →</a>
    </div>
  </div>

  <!-- 4 ACTION BUTTONS -->
  <div class="ab-grid">
    <button class="ab" onclick="openQ()">
      <span class="ab-icon">🎯</span>
      <span class="ab-lbl ab1l">¿QUÉ EQUIPO?</span>
      <span class="ab-sub ab1s">Quiz personalizado</span>
    </button>
    <button class="ab" onclick="openC()">
      <span class="ab-icon">📊</span>
      <span class="ab-lbl ab2l">MI TALLA</span>
      <span class="ab-sub ab2s">Calculadora</span>
    </button>
    <button class="ab" onclick="openB()">
      <span class="ab-icon">💰</span>
      <span class="ab-lbl ab3l">PRESUPUESTO</span>
      <span class="ab-sub ab3s">Kit completo</span>
    </button>
    <button class="ab" onclick="openF()">
      <span class="ab-icon">👔</span>
      <span class="ab-lbl ab4l">ASESOR MODA</span>
      <span class="ab-sub ab4s">Ropa y calzado</span>
    </button>
  </div>

  <!-- CATEGORIES -->
  <div class="sec-hdr"><div class="sec-ttl" id="cttl">CATEGORÍAS</div><div class="sec-line"></div></div>
  <div class="cats" id="cats"></div>

  <!-- POPULAR -->
  <div class="sec-hdr" style="margin-top:24px"><div class="sec-ttl" id="pttl">LO MÁS BUSCADO</div><div class="sec-line"></div></div>
  <div class="feat-grid" id="feats"></div>

  <!-- RECOMMENDED -->
  <div class="sec-hdr" style="margin-top:24px">
    <div class="sec-ttl" id="rttl">EQUIPO RECOMENDADO</div>
    <div class="sec-line"></div>
    <div class="sec-tag">COACH PICK</div>
  </div>
  <div class="prod-grid" id="prods"></div>

</div>
</div>

<!-- MODAL: QUIZ -->
<div class="overlay" id="m-quiz">
<div class="mbox">
  <button class="mcls" onclick="closeM('m-quiz')">✕</button>
  <div class="mttl" id="qttl">🎯 ¿QUÉ EQUIPO NECESITO?</div>
  <div class="msub" id="qsub">3 preguntas — recomendación exacta</div>
  <div class="qprog"><div class="qdot done" id="qd1"></div><div class="qdot" id="qd2"></div><div class="qdot" id="qd3"></div></div>
  <div class="qstep on" id="qs1">
    <p style="font-size:12px;color:#ccc;font-family:'Barlow';margin-bottom:10px" id="qq1">¿Cuánto pesas aproximadamente?</p>
    <div class="qopts">
      <button class="qopt" onclick="qa(1,'ligero')">⚡ Ligero — hasta 147 lbs (67 kg)</button>
      <button class="qopt" onclick="qa(1,'mediano')">💪 Mediano — 148 a 175 lbs (68-79 kg)</button>
      <button class="qopt" onclick="qa(1,'pesado')">🏋️ Pesado — 176+ lbs (80+ kg)</button>
    </div>
  </div>
  <div class="qstep" id="qs2">
    <p style="font-size:12px;color:#ccc;font-family:'Barlow';margin-bottom:10px" id="qq2">¿Cuál es tu estilo de boxeo?</p>
    <div class="qopts">
      <button class="qopt" onclick="qa(2,'velocidad')">⚡ Velocidad — me muevo mucho, jab y salida</button>
      <button class="qopt" onclick="qa(2,'fuerza')">💥 Fuerza — entro, presiono y golpeo al cuerpo</button>
      <button class="qopt" onclick="qa(2,'tecnico')">🧠 Técnico — busco ángulos y contraataco</button>
    </div>
  </div>
  <div class="qstep" id="qs3">
    <p style="font-size:12px;color:#ccc;font-family:'Barlow';margin-bottom:10px" id="qq3">¿Cuál es tu nivel?</p>
    <div class="qopts">
      <button class="qopt" onclick="qa(3,'principiante')">🌱 Principiante — menos de 6 meses</button>
      <button class="qopt" onclick="qa(3,'intermedio')">🥊 Intermedio — ya hago sparring</button>
      <button class="qopt" onclick="qa(3,'competidor')">🏆 Competidor — participo en torneos</button>
    </div>
  </div>
  <div class="qstep" id="qs4"><div id="qres"></div></div>
</div>
</div>

<!-- MODAL: CALC -->
<div class="overlay" id="m-calc">
<div class="mbox">
  <button class="mcls" onclick="closeM('m-calc')">✕</button>
  <div class="mttl" id="clttl">📊 CALCULADORA DE TALLA</div>
  <div class="msub" id="clsub">Escribe tu peso — te digo qué tallas necesitas</div>
  <div class="ctog">
    <button class="cunit on" id="u-lbs" onclick="setU('lbs')">LIBRAS</button>
    <button class="cunit" id="u-kg" onclick="setU('kg')">KILOS</button>
  </div>
  <input type="number" class="cinp" id="c-inp" placeholder="Ej: 154" oninput="calcR()" min="40" max="400">
  <div id="c-res"></div>
</div>
</div>

<!-- MODAL: BUDGET -->
<div class="overlay" id="m-budget">
<div class="mbox">
  <button class="mcls" onclick="closeM('m-budget')">✕</button>
  <div class="mttl" id="bgttl">💰 KIT POR PRESUPUESTO</div>
  <div class="msub" id="bgsub">Mueve el slider y armo tu kit</div>
  <div class="bnum" id="bnum">$150</div>
  <input type="range" class="bslider" id="bslider" min="50" max="500" step="25" value="150" oninput="calcB(this.value)">
  <div style="display:flex;justify-content:space-between;font-size:9px;color:var(--muted);margin-bottom:14px"><span>$50</span><span>$500</span></div>
  <div id="bkit"></div>
  <div id="btotal" style="margin-top:10px;padding:11px;background:rgba(212,160,23,.07);border-radius:7px;font-family:'Bebas Neue';font-size:14px;color:var(--gold);letter-spacing:2px;text-align:center"></div>
</div>
</div>

<!-- MODAL: ADMIN POTW -->
<div class="overlay" id="m-adm">
<div class="mbox">
  <button class="mcls" onclick="closeM('m-adm')">✕</button>
  <div class="mttl">🔒 PRODUCTO DE LA SEMANA</div>
  <div class="msub">Solo para el Coach — PIN 1967</div>
  <div id="adm-pin">
    <input type="password" class="cinp" id="a-pin" placeholder="PIN..." onkeydown="if(event.key==='Enter')chkPin()">
    <button onclick="chkPin()" style="width:100%;background:linear-gradient(135deg,#c49010,#f0c030);color:#000;border:none;border-radius:9px;padding:12px;font-weight:900;font-family:'Bebas Neue';font-size:14px;letter-spacing:2px;cursor:pointer;margin-top:7px">ENTRAR</button>
  </div>
  <div id="adm-form" style="display:none">
    <div style="display:flex;flex-direction:column;gap:9px">
      <input type="text" class="cinp" id="a-nm" style="font-size:13px;text-align:left" placeholder="Nombre del producto...">
      <input type="text" class="cinp" id="a-ds" style="font-size:13px;text-align:left" placeholder="Descripción corta...">
      <input type="text" class="cinp" id="a-lk" style="font-size:13px;text-align:left" placeholder="Link Amazon con tu tag...">
      <button onclick="savePW()" style="background:linear-gradient(135deg,#c49010,#f0c030);color:#000;border:none;border-radius:9px;padding:13px;font-weight:900;font-family:'Bebas Neue';font-size:15px;letter-spacing:2px;cursor:pointer">✅ PUBLICAR</button>
      <button onclick="delPW()" style="background:rgba(255,60,60,.08);color:#f88;border:1px solid rgba(255,60,60,.2);border-radius:9px;padding:9px;font-family:'Barlow';font-size:11px;cursor:pointer">🗑️ Quitar producto</button>
    </div>
  </div>
</div>
</div>

<!-- ADMIN BUTTON -->
<button class="adm-btn" onclick="openM('m-adm')">⚙</button>


<!-- MODAL: FASHION ADVISOR -->
<div class="overlay" id="m-fashion">
<div class="mbox">
  <button class="mcls" onclick="closeM('m-fashion')">✕</button>
  <div class="mttl">👔 ASESOR DE MODA</div>
  <div class="msub">Tus medidas → las mejores opciones en Amazon</div>

  <div id="f-form">
    <!-- GENDER -->
    <div style="margin-bottom:12px">
      <div style="font-size:10px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:6px">GÉNERO</div>
      <div style="display:flex;gap:6px">
        <button class="fopt on" id="fg-h" onclick="setFG('hombre',this)">👨 Hombre</button>
        <button class="fopt" id="fg-m" onclick="setFG('mujer',this)">👩 Mujer</button>
        <button class="fopt" id="fg-u" onclick="setFG('unisex',this)">🧑 Unisex</button>
      </div>
    </div>

    <!-- WEIGHT + HEIGHT -->
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px">
      <div>
        <div style="font-size:10px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:4px">PESO (lbs)</div>
        <input type="number" class="cinp" id="f-w" placeholder="154" style="margin:0;font-size:14px" oninput="calcFS()">
      </div>
      <div>
        <div style="font-size:10px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:4px">ALTURA (pies)</div>
        <input type="number" class="cinp" id="f-h" placeholder="5.9" step="0.1" style="margin:0;font-size:14px" oninput="calcFS()">
      </div>
    </div>

    <!-- COLOR -->
    <div style="margin-bottom:12px">
      <div style="font-size:10px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:6px">COLORES FAVORITOS</div>
      <div style="display:flex;flex-wrap:wrap;gap:5px" id="f-colors">
        <button class="fcol" data-c="black" onclick="toggleCol(this)">⬛ Negro</button>
        <button class="fcol" data-c="white" onclick="toggleCol(this)">⬜ Blanco</button>
        <button class="fcol" data-c="blue" onclick="toggleCol(this)">🔵 Azul</button>
        <button class="fcol" data-c="gray" onclick="toggleCol(this)">🩶 Gris</button>
        <button class="fcol" data-c="red" onclick="toggleCol(this)">🔴 Rojo</button>
        <button class="fcol" data-c="green" onclick="toggleCol(this)">🟢 Verde</button>
        <button class="fcol" data-c="navy" onclick="toggleCol(this)">🌑 Marino</button>
        <button class="fcol" data-c="beige" onclick="toggleCol(this)">🟤 Beige</button>
      </div>
    </div>

    <!-- STYLE -->
    <div style="margin-bottom:16px">
      <div style="font-size:10px;color:var(--muted);font-family:'Barlow Condensed';letter-spacing:2px;margin-bottom:6px">ESTILO</div>
      <div style="display:flex;gap:6px;flex-wrap:wrap">
        <button class="fopt on" id="fs-cas" onclick="setFS('casual',this)">👕 Casual</button>
        <button class="fopt" id="fs-dep" onclick="setFS('sport',this)">🏃 Deportivo</button>
        <button class="fopt" id="fs-for" onclick="setFS('formal',this)">👔 Formal</button>
      </div>
    </div>

    <!-- SIZE PREVIEW -->
    <div id="f-size-preview" style="display:none;background:rgba(212,160,23,.06);border:1px solid rgba(212,160,23,.2);border-radius:8px;padding:10px;margin-bottom:14px;font-family:'Barlow Condensed';font-size:12px;color:var(--gold);letter-spacing:1px;text-align:center"></div>

    <button onclick="showFashion()" style="width:100%;background:linear-gradient(135deg,#c49010,#f0c030);color:#000;border:none;border-radius:10px;padding:13px;font-weight:900;font-family:'Bebas Neue';font-size:15px;letter-spacing:2px;cursor:pointer">👔 VER MIS OPCIONES EN AMAZON →</button>
  </div>

  <!-- RESULTS -->
  <div id="f-results" style="display:none">
    <button onclick="document.getElementById('f-form').style.display='block';document.getElementById('f-results').style.display='none'" style="background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.08);color:var(--muted);border-radius:7px;padding:6px 12px;font-size:11px;cursor:pointer;margin-bottom:14px;font-family:'Barlow'">← Editar medidas</button>
    <div style="font-family:'Bebas Neue';font-size:14px;color:var(--gold);letter-spacing:2px;margin-bottom:12px" id="f-res-ttl">TUS OPCIONES EN AMAZON</div>
    <div id="f-res-grid"></div>
  </div>
</div>
</div>

<!-- FOOTER -->
<footer>
  <div class="flogo">CBA STORE</div>
  <div class="flinks">
    <a href="https://cbagym.com">🥊 cbagym.com</a>
    <a href="https://wa.me/17027234009">💬 WhatsApp</a>
    <a href="tel:+17027234009">📞 (702) 723-4009</a>
  </div>
  <div class="fcopy" id="fcp">CBA Store · boxingcba.com · Amazon Affiliate Store</div>
</footer>

<script>
var TAG = 'osvimerboxing-20';
var L = 'es';

// ── SEARCH ──────────────────────────────────────────────────
function goSearch(q) {
  q = (q || '').trim();
  if (!q) return;
  window.open('https://www.amazon.com/s?k=' + encodeURIComponent(q) + '&tag=' + TAG, '_blank');
}

// ── DATA ────────────────────────────────────────────────────
var CATS = {
  es:[{i:'🥊',n:'Boxeo',q:'boxing equipment'},{i:'👟',n:'Calzado',q:'boxing shoes'},{i:'💪',n:'Fitness',q:'fitness equipment gym'},{i:'👕',n:'Ropa',q:'athletic clothing sports'},{i:'📱',n:'Electrónicos',q:'electronics smartphones'},{i:'🏠',n:'Hogar',q:'home kitchen appliances'},{i:'💄',n:'Belleza',q:'beauty skincare'},{i:'🎮',n:'Gaming',q:'video games console'},{i:'📚',n:'Libros',q:'books bestsellers'},{i:'🧴',n:'Salud',q:'health supplements protein'},{i:'👶',n:'Niños',q:'kids toys children'},{i:'🐾',n:'Mascotas',q:'pet supplies'}],
  en:[{i:'🥊',n:'Boxing',q:'boxing equipment'},{i:'👟',n:'Shoes',q:'boxing shoes'},{i:'💪',n:'Fitness',q:'fitness equipment gym'},{i:'👕',n:'Clothing',q:'athletic clothing sports'},{i:'📱',n:'Electronics',q:'electronics smartphones'},{i:'🏠',n:'Home',q:'home kitchen appliances'},{i:'💄',n:'Beauty',q:'beauty skincare'},{i:'🎮',n:'Gaming',q:'video games console'},{i:'📚',n:'Books',q:'books bestsellers'},{i:'🧴',n:'Health',q:'health supplements protein'},{i:'👶',n:'Kids',q:'kids toys children'},{i:'🐾',n:'Pets',q:'pet supplies'}]
};

var FEATS = {
  es:[{i:'🪖',n:'Cascos',s:'USA Boxing',q:'Adidas boxing headgear USA Boxing'},{i:'🩹',n:'Vendas',s:'180cm+',q:'boxing hand wraps 180 inches'},{i:'😁',n:'Protector',s:'Doble',q:'Everlast double mouthguard'},{i:'🏋️',n:'Pesas',s:'Home gym',q:'dumbbells weight set'},{i:'📺',n:'Smart TV',s:'4K',q:'4K Smart TV'},{i:'💻',n:'Laptops',s:'Deals',q:'laptop computer deals'},{i:'🎧',n:'Auriculares',s:'Wireless',q:'wireless headphones bluetooth'},{i:'⌚',n:'Smartwatch',s:'Sport',q:'smartwatch fitness'},{i:'🧴',n:'Proteína',s:'Suplementos',q:'protein powder supplements'},{i:'🛋️',n:'Muebles',s:'Hogar',q:'furniture home decor'},{i:'🎽',n:'Deportiva',s:'Adidas',q:'adidas athletic wear'},{i:'🍳',n:'Cocina',s:'Electrodomésticos',q:'kitchen appliances cookware'}],
  en:[{i:'🪖',n:'Headgear',s:'USA Boxing',q:'Adidas boxing headgear USA Boxing'},{i:'🩹',n:'Hand Wraps',s:'180"+',q:'boxing hand wraps 180 inches'},{i:'😁',n:'Mouthguard',s:'Double',q:'Everlast double mouthguard'},{i:'🏋️',n:'Weights',s:'Home gym',q:'dumbbells weight set'},{i:'📺',n:'Smart TV',s:'4K',q:'4K Smart TV'},{i:'💻',n:'Laptops',s:'Deals',q:'laptop computer deals'},{i:'🎧',n:'Headphones',s:'Wireless',q:'wireless headphones bluetooth'},{i:'⌚',n:'Smartwatch',s:'Sport',q:'smartwatch fitness'},{i:'🧴',n:'Protein',s:'Supplements',q:'protein powder supplements'},{i:'🛋️',n:'Furniture',s:'Home',q:'furniture home decor'},{i:'🎽',n:'Sportswear',s:'Adidas',q:'adidas athletic wear'},{i:'🍳',n:'Kitchen',s:'Appliances',q:'kitchen appliances cookware'}]
};

var PRODS = {
  es:[{b:'COACH PICK',e:'🥊',n:'GUANTES ADIDAS',d:'USA Boxing & IBA Approved.',a:'B0F4NM14Y5'},{b:'COACH PICK',e:'🪖',n:'CASCO ADIDAS',d:'USA Boxing Approved. Espuma dura.',a:'B0B3R2V65J'},{b:'RECOMENDADO',e:'😁',n:'PROTECTOR DOBLE',d:'Everlast doble — superior e inferior.',a:'B00076ZDQ8'},{b:'RECOMENDADO',e:'👟',n:'ADIDAS BOX HOG 4',d:'Suela plana. Para velocidad.',a:'B0CMXQ7KSK'},{b:'RECOMENDADO',e:'👟',n:'NIKE HYPERKO 2',d:'Soporte máximo. Para pesados.',a:'B0D1CK8F51'},{b:'RECOMENDADO',e:'👕',n:'SHORT ADIDAS',d:'IBA Approved para torneos.',a:'B081ZRYM6Z'}],
  en:[{b:'COACH PICK',e:'🥊',n:'ADIDAS GLOVES',d:'USA Boxing & IBA Approved.',a:'B0F4NM14Y5'},{b:'COACH PICK',e:'🪖',n:'ADIDAS HEADGEAR',d:'USA Boxing Approved. Hard foam.',a:'B0B3R2V65J'},{b:'RECOMMENDED',e:'😁',n:'DOUBLE MOUTHGUARD',d:'Everlast double — upper & lower.',a:'B00076ZDQ8'},{b:'RECOMMENDED',e:'👟',n:'ADIDAS BOX HOG 4',d:'Flat sole. For speed.',a:'B0CMXQ7KSK'},{b:'RECOMMENDED',e:'👟',n:'NIKE HYPERKO 2',d:'Max ankle support. Heavyweights.',a:'B0D1CK8F51'},{b:'RECOMMENDED',e:'👕',n:'ADIDAS SHORTS',d:'IBA Approved for tournaments.',a:'B081ZRYM6Z'}]
};

var T = {
  es:{htag:'🥊 TIENDA OFICIAL — CUBAN BOXING ACADEMY',ht1:'ENCUENTRA',ht2:'TODO',hsub:'Busca cualquier producto y cómpralo en Amazon. Boxeo, electrónicos, ropa, hogar y más.',hgo:'BUSCAR →',hhint:'Presiona Enter — te lleva directo a Amazon','cam-t':'TOMAR FOTO','gal-t':'SUBIR FOTO',cttl:'CATEGORÍAS',pttl:'LO MÁS BUSCADO',rttl:'EQUIPO RECOMENDADO',ab1l:'¿QUÉ EQUIPO?',ab1s:'Quiz personalizado',ab2l:'MI TALLA',ab2s:'Calculadora',ab3l:'PRESUPUESTO',ab3s:'Kit completo',qttl:'🎯 ¿QUÉ EQUIPO NECESITO?',qsub:'3 preguntas — recomendación exacta',qq1:'¿Cuánto pesas aproximadamente?',qq2:'¿Cuál es tu estilo de boxeo?',qq3:'¿Cuál es tu nivel?',clttl:'📊 CALCULADORA DE TALLA',clsub:'Escribe tu peso — te digo qué tallas necesitas',bgttl:'💰 KIT POR PRESUPUESTO',bgsub:'Mueve el slider y armo tu kit',fcp:'CBA Store · boxingcba.com · Amazon Affiliate Store'},
  en:{htag:'🥊 OFFICIAL STORE — CUBAN BOXING ACADEMY',ht1:'FIND',ht2:'EVERYTHING',hsub:'Search any product on Amazon. Boxing, electronics, clothing, home and more.',hgo:'SEARCH →',hhint:'Press Enter — goes directly to Amazon','cam-t':'TAKE PHOTO','gal-t':'UPLOAD PHOTO',cttl:'CATEGORIES',pttl:'MOST SEARCHED',rttl:'RECOMMENDED GEAR',ab1l:'WHAT GEAR?',ab1s:'Personalized quiz',ab2l:'MY SIZE',ab2s:'Calculator',ab3l:'BUDGET',ab3s:'Complete kit',qttl:'🎯 WHAT GEAR DO I NEED?',qsub:'3 questions — exact recommendation',qq1:'How much do you weigh?',qq2:'What is your boxing style?',qq3:'What is your level?',clttl:'📊 SIZE CALCULATOR',clsub:'Enter your weight — I tell you your sizes',bgttl:'💰 KIT BY BUDGET',bgsub:'Move the slider and I build your kit',fcp:'CBA Store · boxingcba.com · Amazon Affiliate Store'}
};

// ── LANGUAGE ────────────────────────────────────────────────
function setL(l, btn) {
  L = l;
  document.querySelectorAll('.lng').forEach(function(b) { b.classList.remove('active'); });
  if (btn) btn.classList.add('active');
  var t = T[l];
  Object.keys(t).forEach(function(k) {
    var el = document.getElementById(k);
    if (el) el.textContent = t[k];
    document.querySelectorAll('.' + k).forEach(function(e) { e.textContent = t[k]; });
  });
  document.getElementById('hero-inp').placeholder = l === 'es' ? '¿Qué estás buscando?' : 'What are you looking for?';
  document.getElementById('h-inp').placeholder = l === 'es' ? 'Busca cualquier producto en Amazon...' : 'Search any product on Amazon...';
  render();
}

// ── RENDER ──────────────────────────────────────────────────
function render() {
  var cg = document.getElementById('cats');
  cg.innerHTML = '';
  CATS[L].forEach(function(c) {
    var btn = document.createElement('button');
    btn.className = 'cat';
    btn.innerHTML = c.i + ' ' + c.n;
    btn.onclick = function() { goSearch(c.q); };
    cg.appendChild(btn);
  });

  var fg = document.getElementById('feats');
  fg.innerHTML = '';
  FEATS[L].forEach(function(f) {
    var d = document.createElement('div');
    d.className = 'feat';
    d.innerHTML = '<div class="feat-ic">' + f.i + '</div><div class="feat-nm">' + f.n + '</div><div class="feat-sb">' + f.s + '</div>';
    d.onclick = function() { goSearch(f.q); };
    fg.appendChild(d);
  });

  var pg = document.getElementById('prods');
  pg.innerHTML = '';
  PRODS[L].forEach(function(p) {
    var a = document.createElement('a');
    a.className = 'pc';
    a.href = 'https://www.amazon.com/dp/' + p.a + '?tag=' + TAG;
    a.target = '_blank';
    var wa = 'https://wa.me/?text=' + encodeURIComponent('Mira este equipo 👊 https://www.amazon.com/dp/' + p.a + '?tag=' + TAG);
    a.innerHTML = '<div class="pc-badge">' + p.b + '</div><a class="pc-share" href="' + wa + '" target="_blank" onclick="event.stopPropagation()">💬</a><div class="pc-body"><span class="pc-em">' + p.e + '</span><div class="pc-nm">' + p.n + '</div><div class="pc-ds">' + p.d + '</div><div class="pc-cta">🛒 VER EN AMAZON →</div></div>';
    pg.appendChild(a);
  });
}

// ── MODAL HELPERS ────────────────────────────────────────────
function openM(id) { document.getElementById(id).classList.add('on'); }
function closeM(id) {
  document.getElementById(id).classList.remove('on');
  if (id === 'm-adm') {
    document.getElementById('adm-pin').style.display = 'block';
    document.getElementById('adm-form').style.display = 'none';
    document.getElementById('a-pin').value = '';
  }
}
document.querySelectorAll('.overlay').forEach(function(o) {
  o.addEventListener('click', function(e) { if (e.target === o) closeM(o.id); });
});

// ── FEATURE 1: QUIZ ─────────────────────────────────────────
var QA = {};
function openQ() {
  QA = {};
  showQS(1);
  openM('m-quiz');
}
function showQS(n) {
  document.querySelectorAll('.qstep').forEach(function(s) { s.classList.remove('on'); });
  var el = document.getElementById('qs' + n);
  if (el) el.classList.add('on');
  for (var i = 1; i <= 3; i++) {
    var d = document.getElementById('qd' + i);
    if (d) d.classList.toggle('done', i <= n);
  }
}
function qa(step, val) {
  QA['s' + step] = val;
  document.querySelectorAll('#qs' + step + ' .qopt').forEach(function(b) { b.classList.remove('sel'); });
  event.target.classList.add('sel');
  var next = step + 1;
  setTimeout(function() {
    if (next <= 3) showQS(next);
    else showQR();
  }, 280);
}
function mkRR(e, nm, sub, asin) {
  var lnk = asin ? 'https://www.amazon.com/dp/' + asin + '?tag=' + TAG : '#';
  return '<div style="display:flex;align-items:center;gap:9px;padding:9px;background:rgba(255,255,255,.03);border-radius:7px;margin-bottom:5px"><span style="font-size:20px">' + e + '</span><div style="flex:1"><div style="font-size:11px;font-weight:900;color:#fff;font-family:Barlow Condensed,sans-serif;letter-spacing:1px">' + nm + '</div><div style="font-size:9px;color:var(--muted)">' + sub + '</div></div>' + (asin ? '<a href="' + lnk + '" target="_blank" style="background:rgba(255,153,0,.15);color:#f90;border-radius:5px;padding:4px 7px;font-size:9px;font-weight:900;text-decoration:none;font-family:Barlow Condensed,sans-serif;letter-spacing:1px;white-space:nowrap">VER →</a>' : '') + '</div>';
}
function showQR() {
  showQS(4);
  var w = QA.s1, s = QA.s2, lv = QA.s3;
  var gs = w === 'ligero' ? '10oz - 12oz' : w === 'mediano' ? '12oz - 14oz' : '14oz - 16oz';
  var sA = s === 'fuerza' ? 'B0D1CK8F51' : 'B0CMXQ7KSK';
  var sN = s === 'fuerza' ? 'Nike Hyperko 2' : 'Adidas Box Hog 4';
  var html = '<div style="font-family:Bebas Neue,sans-serif;font-size:16px;color:var(--gold);letter-spacing:2px;margin-bottom:14px">🎯 TU KIT RECOMENDADO</div>';
  html += mkRR('🥊', 'Guantes Adidas ' + gs, 'USA Boxing Approved', 'B0F4NM14Y5');
  html += mkRR('🩹', 'Vendas 180cm', '4-5 vueltas muñeca y metacarpiano', null);
  html += mkRR('😁', 'Protector doble Everlast', 'Superior e inferior', 'B00076ZDQ8');
  html += mkRR('👟', sN, 'Mejor para tu estilo', sA);
  if (lv !== 'principiante') html += mkRR('🪖', 'Casco Adidas USA Boxing', 'Para sparring y torneos', 'B0B3R2V65J');
  if (lv === 'competidor') html += mkRR('👕', 'Short Adidas IBA', 'Uniforme oficial', 'B081ZRYM6Z');
  html += '<button onclick="closeM(\'m-quiz\');openQ()" style="margin-top:12px;width:100%;background:rgba(212,160,23,.08);border:1px solid rgba(212,160,23,.2);color:var(--gold);border-radius:7px;padding:9px;font-family:Barlow Condensed,sans-serif;font-size:11px;letter-spacing:1px;cursor:pointer">🔄 REPETIR QUIZ</button>';
  document.getElementById('qres').innerHTML = html;
}

// ── FEATURE 2: CALCULATOR ────────────────────────────────────
var CU = 'lbs';
function openC() {
  document.getElementById('c-res').innerHTML = '';
  document.getElementById('c-inp').value = '';
  openM('m-calc');
}
function setU(u) {
  CU = u;
  document.getElementById('u-lbs').classList.toggle('on', u === 'lbs');
  document.getElementById('u-kg').classList.toggle('on', u === 'kg');
  document.getElementById('c-inp').value = '';
  document.getElementById('c-res').innerHTML = '';
}
function calcR() {
  var raw = parseFloat(document.getElementById('c-inp').value);
  if (!raw || raw < 40) { document.getElementById('c-res').innerHTML = ''; return; }
  var lb = CU === 'lbs' ? raw : raw * 2.205;
  var glv = lb <= 130 ? '8oz - 10oz' : lb <= 150 ? '10oz - 12oz' : lb <= 170 ? '12oz - 14oz' : lb <= 200 ? '14oz - 16oz' : '16oz - 18oz';
  var hel = lb <= 130 ? 'S' : lb <= 160 ? 'M' : lb <= 185 ? 'L' : 'XL';
  var shA = lb <= 160 ? 'B0CMXQ7KSK' : 'B0D1CK8F51';
  var shN = lb <= 160 ? 'Adidas Box Hog 4' : 'Nike Hyperko 2';
  function cr(lbl, val, asin) {
    return '<div class="crow"><span class="clbl">' + lbl + '</span><div style="display:flex;align-items:center;gap:7px"><span class="cval">' + val + '</span>' + (asin ? '<a href="https://www.amazon.com/dp/' + asin + '?tag=' + TAG + '" target="_blank" style="background:rgba(255,153,0,.15);color:#f90;border-radius:4px;padding:2px 6px;font-size:9px;font-weight:900;text-decoration:none">BUY</a>' : '') + '</div></div>';
  }
  document.getElementById('c-res').innerHTML = '<div style="background:rgba(0,0,0,.25);border-radius:9px;padding:12px;margin-top:8px">' + cr('🥊 Guantes', glv, 'B0F4NM14Y5') + cr('🩹 Vendas', '180cm+', null) + cr('🪖 Casco', hel, 'B0B3R2V65J') + cr('👟 Calzado', shN, shA) + cr('😁 Protector', 'Doble', 'B00076ZDQ8') + '</div>';
}

// ── FEATURE 3: BUDGET ────────────────────────────────────────
var KIT = [
  {e:'🩹',n:'Vendas 180cm',p:15,a:null,q:'boxing hand wraps 180 inches'},
  {e:'😁',n:'Protector doble',p:20,a:'B00076ZDQ8'},
  {e:'🥊',n:'Guantes 12oz',p:55,a:'B0F4NM14Y5'},
  {e:'🪖',n:'Casco Adidas',p:80,a:'B0B3R2V65J'},
  {e:'👟',n:'Adidas Box Hog 4',p:90,a:'B0CMXQ7KSK'},
  {e:'👕',n:'Short Adidas IBA',p:45,a:'B081ZRYM6Z'},
  {e:'🥊',n:'Guantes competencia',p:120,a:'B0F4NM14Y5'}
];
function openB() { calcB(150); document.getElementById('bslider').value = 150; openM('m-budget'); }
function calcB(v) {
  v = parseInt(v);
  document.getElementById('bnum').textContent = '$' + v;
  var total = 0, html = '';
  KIT.forEach(function(item) {
    var ok = (total + item.p) <= v;
    if (ok) total += item.p;
    var lnk = item.a ? ('https://www.amazon.com/dp/' + item.a + '?tag=' + TAG) : ('https://www.amazon.com/s?k=' + encodeURIComponent(item.q || item.n) + '&tag=' + TAG);
    html += '<div class="kitem ' + (ok ? 'in' : 'out') + '"><span class="kim">' + item.e + '</span><div class="kinf"><div class="knm">' + item.n + '</div><div class="kpr">~$' + item.p + '</div></div>' + (ok ? '<a href="' + lnk + '" target="_blank" style="background:rgba(255,153,0,.15);color:#f90;border-radius:5px;padding:4px 7px;font-size:9px;font-weight:900;text-decoration:none;font-family:Barlow Condensed,sans-serif;white-space:nowrap">VER</a>' : '') + '<span style="font-size:14px">' + (ok ? '✅' : '⬜') + '</span></div>';
  });
  document.getElementById('bkit').innerHTML = html;
  document.getElementById('btotal').textContent = total > 0 ? '💰 Total estimado: $' + total : 'Aumenta el presupuesto';
}

// ── FEATURE 4: SHARE (WhatsApp) — injected in render() ──────
// Already included in prod card HTML above

// ── FEATURE 5: PRODUCT OF THE WEEK ─────────────────────────
function loadPW() {
  try {
    var d = localStorage.getItem('cba_pw');
    if (!d) return;
    var p = JSON.parse(d);
    if (p && p.nm && p.lk) {
      document.getElementById('pw-nm').textContent = p.nm;
      document.getElementById('pw-ds').textContent = p.ds || '';
      document.getElementById('pw-lk').href = p.lk;
      document.getElementById('potw').style.display = 'block';
    }
  } catch(e) {}
}
function chkPin() {
  if (document.getElementById('a-pin').value === '1967') {
    document.getElementById('adm-pin').style.display = 'none';
    document.getElementById('adm-form').style.display = 'block';
    try {
      var ex = JSON.parse(localStorage.getItem('cba_pw') || '{}');
      if (ex.nm) document.getElementById('a-nm').value = ex.nm;
      if (ex.ds) document.getElementById('a-ds').value = ex.ds;
      if (ex.lk) document.getElementById('a-lk').value = ex.lk;
    } catch(e) {}
  } else {
    alert('PIN incorrecto');
  }
}
function savePW() {
  var nm = document.getElementById('a-nm').value.trim();
  var ds = document.getElementById('a-ds').value.trim();
  var lk = document.getElementById('a-lk').value.trim();
  if (!nm || !lk) { alert('Nombre y link son obligatorios'); return; }
  localStorage.setItem('cba_pw', JSON.stringify({nm:nm, ds:ds, lk:lk}));
  loadPW();
  closeM('m-adm');
}
function delPW() {
  localStorage.removeItem('cba_pw');
  document.getElementById('potw').style.display = 'none';
  closeM('m-adm');
}

// ── VISUAL SEARCH ────────────────────────────────────────────
var aiName = '';
function openCam() { document.getElementById('f-cam').click(); }
function openGal() { document.getElementById('f-gal').click(); }
function doVS(inp) {
  var file = inp.files[0];
  if (!file) return;
  inp.value = '';
  var reader = new FileReader();
  reader.onload = function(e) {
    var img = new Image();
    img.onload = function() {
      var c = document.createElement('canvas');
      var MAX = 600, w = img.width, h = img.height;
      if (w > MAX || h > MAX) {
        if (w > h) { h = Math.round(h * MAX / w); w = MAX; }
        else { w = Math.round(w * MAX / h); h = MAX; }
      }
      c.width = w; c.height = h;
      c.getContext('2d').drawImage(img, 0, 0, w, h);
      analyzeImg(c.toDataURL('image/jpeg', 0.7).split(',')[1]);
    };
    img.src = e.target.result;
  };
  reader.readAsDataURL(file);
}
function analyzeImg(b64) {
  document.getElementById('ai-ov').classList.add('on');
  document.getElementById('ai-res').style.display = 'none';
  fetch('https://api-inference.huggingface.co/models/google/vit-base-patch16-224', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({inputs: b64})
  })
  .then(function(r) { return r.json(); })
  .then(function(data) {
    document.getElementById('ai-ov').classList.remove('on');
    if (Array.isArray(data) && data.length > 0 && data[0].label) {
      aiName = data[0].label.split(',')[0].trim();
      aiName = aiName.charAt(0).toUpperCase() + aiName.slice(1);
      document.getElementById('ai-name').textContent = aiName;
      document.getElementById('ai-res').style.display = 'block';
    } else if (data && data.error) {
      document.getElementById('ai-st').textContent = 'Cargando modelo...';
      setTimeout(function() { analyzeImg(b64); }, 10000);
    } else {
      alert('No se identificó el producto. Usa la búsqueda de texto.');
    }
  })
  .catch(function() {
    document.getElementById('ai-ov').classList.remove('on');
    alert('Error de conexión. Usa la búsqueda de texto.');
  });
}
function doAISearch() { document.getElementById('ai-res').style.display = 'none'; goSearch(aiName); }
function closeAI() { document.getElementById('ai-res').style.display = 'none'; aiName = ''; }


// ── FASHION ADVISOR ─────────────────────────────────────────
var FG = 'hombre', FS = 'casual', FC = [];

function setFG(g, btn) {
  FG = g;
  document.querySelectorAll('#fg-h,#fg-m,#fg-u').forEach(function(b){ b.classList.remove('on'); });
  btn.classList.add('on');
  calcFS();
}
function setFS(s, btn) {
  FS = s;
  document.querySelectorAll('#fs-cas,#fs-dep,#fs-for').forEach(function(b){ b.classList.remove('on'); });
  btn.classList.add('on');
}
function toggleCol(btn) {
  btn.classList.toggle('on');
  var c = btn.getAttribute('data-c');
  var idx = FC.indexOf(c);
  if(idx >= 0) FC.splice(idx, 1); else FC.push(c);
}
function openF() {
  document.getElementById('f-form').style.display = 'block';
  document.getElementById('f-results').style.display = 'none';
  openM('m-fashion');
}
function calcFS() {
  var w = parseFloat(document.getElementById('f-w').value);
  var h = parseFloat(document.getElementById('f-h').value);
  if(!w || !h) { document.getElementById('f-size-preview').style.display='none'; return; }
  var sz = getFashionSize(w, h);
  document.getElementById('f-size-preview').style.display = 'block';
  document.getElementById('f-size-preview').textContent = 'Talla estimada: ' + sz.shirt + ' · Zapato: ' + sz.shoe + ' · Pantalón: ' + sz.pants;
}
function getFashionSize(lbs, ft) {
  var bmi = (lbs / (ft * ft)) * 703;
  var shirt, pants, shoe;
  // Shirt/top size
  if(bmi < 18.5) shirt = 'XS - S';
  else if(bmi < 22) shirt = 'S - M';
  else if(bmi < 26) shirt = 'M - L';
  else if(bmi < 30) shirt = 'L - XL';
  else shirt = 'XL - 2XL';
  // Pants waist estimate
  var waist = Math.round(bmi * 1.1 + 10);
  pants = waist + '" waist';
  // Shoe size estimate from height
  var inches = ft * 12;
  var shoeNum = Math.round((inches - 50) * 0.6 + 7);
  shoe = 'US ' + shoeNum + ' - ' + (shoeNum + 1);
  return {shirt:shirt, pants:pants, shoe:shoe, shoeNum:shoeNum, waist:waist};
}
function buildQ(base, size, color, gender) {
  var gStr = gender === 'hombre' ? 'men' : gender === 'mujer' ? 'women' : '';
  var cStr = color.length > 0 ? color.slice(0,2).join(' ') : '';
  return [gStr, cStr, base, size].filter(Boolean).join(' ').trim();
}
function showFashion() {
  var w = parseFloat(document.getElementById('f-w').value);
  var h = parseFloat(document.getElementById('f-h').value);
  if(!w || !h) { alert('Escribe tu peso y altura primero'); return; }
  var sz = getFashionSize(w, h);
  var g = FG, col = FC.length > 0 ? FC : ['black','blue'];
  var colStr = col.slice(0,2).join(' ');
  var gStr = g === 'hombre' ? "men's" : g === 'mujer' ? "women's" : '';
  var stStr = FS === 'sport' ? 'athletic workout' : FS === 'formal' ? 'dress formal' : 'casual everyday';
  var cats = [
    {icon:'👕', name:'Camisetas / Tops', q:gStr+' '+colStr+' '+stStr+' shirt '+sz.shirt, detail:'Talla ' + sz.shirt},
    {icon:'👖', name:'Pantalones', q:gStr+' '+colStr+' '+stStr+' pants '+sz.waist+' waist', detail:'Cintura ~' + sz.waist+'"'},
    {icon:'🧥', name:'Chaquetas / Hoodies', q:gStr+' '+colStr+' '+stStr+' jacket hoodie size '+sz.shirt, detail:'Talla ' + sz.shirt},
    {icon:'👟', name:'Zapatos / Sneakers', q:gStr+' '+colStr+' '+stStr+' shoes size '+sz.shoeNum, detail:'Talla US ' + sz.shoeNum+'-'+(sz.shoeNum+1)},
    {icon:'🩲', name:'Ropa Interior', q:gStr+' '+stStr+' underwear size '+sz.shirt.split(' - ')[0], detail:'Talla ' + sz.shirt.split(' - ')[0]},
    {icon:'🧦', name:'Calcetines / Medias', q:gStr+' '+stStr+' socks', detail:'Talla estándar'},
    {icon:'🎒', name:'Mochilas / Accesorios', q:gStr+' '+colStr+' backpack bag '+stStr, detail:'Color: '+colStr},
    {icon:'🕶️', name:'Gafas / Gorras', q:gStr+' '+colStr+' cap hat sunglasses '+stStr, detail:'Accesorios'}
  ];
  var html = '';
  cats.forEach(function(cat) {
    var url = 'https://www.amazon.com/s?k=' + encodeURIComponent(cat.q) + '&tag=' + TAG;
    html += '<a class="fcat" href="'+url+'" target="_blank">';
    html += '<div class="fcat-hdr"><span class="fcat-icon">'+cat.icon+'</span><span class="fcat-name">'+cat.name+'</span><span class="fcat-cta">VER EN AMAZON →</span></div>';
    html += '<div class="fcat-detail">'+cat.detail+' · '+colStr+' · '+stStr+'</div>';
    html += '</a>';
  });
  document.getElementById('f-res-ttl').textContent = 'TU KIT DE ROPA — TALLA ' + sz.shirt;
  document.getElementById('f-res-grid').innerHTML = html;
  document.getElementById('f-form').style.display = 'none';
  document.getElementById('f-results').style.display = 'block';
}

// ── INIT ─────────────────────────────────────────────────────
render();
loadPW();
</script>
</body>
</html>
